export const list = document.querySelector("#phone");
export const body = document.querySelector("body");
export const main = document.querySelector("#main") as HTMLDivElement;
export const productCrad = document.querySelector(
  "#products"
) as HTMLDivElement;
export const productTitle = document.querySelector(
  "#productTitle"
) as HTMLInputElement;
export const addCartButton = document.querySelector(
  ".addCart"
) as HTMLButtonElement;
